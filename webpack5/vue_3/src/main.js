import { createApp } from 'vue'
import App from './App.vue'
const path = require("path")
const fwdStream = require('fwd-stream')
console.log("我在这里这里",jQuery('#item'),path,fwdStream,"====")
createApp(App).mount('#app')
